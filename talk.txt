/**
 * Сложность кода с базой
 * Самая долгая операция распарсить json
 * Производительность первого обращения к базе не быстрее чем N*log(N)
 * Только одна сортировка
 * Потеря на конвертации данных в базу и обратно
 * onSaveInstanceState(bundle) придётся вычитать всю базу чтобы восстановиться life cycle
 */

1.  Performance
2.  onSaveInstance (насколько большие данные)
3.  Удобство пользования Kotlin vs База
3.1 Мапинг POJO в базу, много сущностей
